import { CenturyPipePipe } from './century-pipe.pipe';

describe('CenturyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CenturyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
